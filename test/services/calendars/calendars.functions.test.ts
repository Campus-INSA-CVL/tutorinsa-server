import {
  createSlots,
  createUTCTime,
} from '../../../src/services/calendars/calendars.functions'

describe("'calendar.functions'", () => {
  describe("'create-slots' function", () => {
    it.todo('should create slot')
  })
  describe("'create-UTC-time' function", () => {
    it.todo('should throw an error if the date is invalide')
    it.todo("should return a new 1970's date")
  })
})
