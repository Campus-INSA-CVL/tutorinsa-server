description:
produces:
  - application/json
operations:
  find:
    summary: Finds the calendar
    description: Retrive the calendar
    responses:
      200:
        description: successful operation
        content:
          application/json:
            schema:
              type: object
              properties:
                total:
                  type: integer
                limit:
                  type: integer
                skip:
                  type: interger
                data:
                  type: array
                  items:
                    $ref: "#/components/schemas/findGetCalendars"
            example:
              total: 1
              limit: 10
              skip: 0
              data:

  get:
    summary: Get one day for one room
    description: Retrive one day for one room
    parameters:
      - name: _id
        in: path
        description: ID of calendar that need to be fetch
        required: true
        schema:
          type: string
    responses:
      200:
        description: successful operation
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/findGetCalendars"
            example:
      400:
        $ref: "#/components/responses/BadRequest"

  create:
    summary: Disallow for external
    parameters:
    requestBody:
      description: Calendar object that will be created
      content:
        json:
          schema:
            $ref: "#/components/schemas/calendar"
          example:
      required: true
    responses:
      201:
        description: created
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/findGetCalendars"
            example:
      403:
        $ref: "#/components/responses/Forbidden"
      security:
        - bearerAuth: []

  update:
    summary: Not allowed
    description: Not allowed
    parameters:
    requestBody:
    responses:
      405:
        $ref: "#/components/responses/NotAllowed"

  patch:
    summary: Update a calendar
    parameters:
      - name: _id
        in: path
        description: ID of calendar that need to be fetch
        required: true
        schema:
          type: string
    requestBody:
      description: Calendar object that need to be updated
      content:
        json:
          schema:
            $ref: "#/components/schemas/calendar"
          example:
      required: true
    responses:
      200:
        description: updated
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/findGetCalendars"
            example:
      400:
        $ref: "#/components/responses/BadRequest"
      403:
        $ref: "#/components/responses/Forbidden"
      404:
        $ref: "#/components/responses/NotFound"
    security:
      - bearerAuth: []

  remove:
    summary: Disallow for external
    parameters:
      - name: _id
        in: path
        description: ID of calendar that need to be fetch
        required: true
        schema:
          type: string
    responses:
      200:
        description: deleted
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/findGetCalendars"
            example:
      400:
        $ref: "#/components/responses/BadRequest"
      403:
        $ref: "#/components/responses/Forbidden"
      404:
        $ref: "#/components/responses/NotFound"
    security:
      - bearerAuth: []
